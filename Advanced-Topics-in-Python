Advanced Topics in Python
===

## Iteration Notions
我们已经学习了如何遍历 *表 (list) * 中的元素，现在我们将学习以下三个点内容：
- 遍历更复杂的数据结构中的元素（如：dictionary）
- 动态的修改表（list），通过（list comprehensions）
- 切分表(list），通过表划分（slicing）

### Iterators for Dictionarys
先从遍历dictionary中的元素讲起，回想一下dictionary其实就是key（键）和value（值）得集合。Python的items()函数可以遍历dictionary中的各个元素，并且返回其 key/value 对。用法就是dictionary.tiems()。注意：这个函数遍历dictionary时并不按照特定的顺序。

### keys() and values()
从上面的操作可以看出items()函数返回一个tuple的序列（array），其中每个tuple包含了dictionary中的一个key/value对。同理：
keys()函数，返回一个包含了dictionary中键key的序列，
values()函数，返回一个由dictionary中值value的序列，

### The 'in' Operator 
